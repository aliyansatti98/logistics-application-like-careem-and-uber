<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.online.boxprologistics.pvt.ltd">
    <!--
         The ACCESS_COARSE/FINE_LOCATION permissions are not required to use
         Google Maps Android API v2, but you must specify either coarse or fine
         location permissions for the "MyLocation" functionality.
    -->
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/logoapp"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".courierr2"></activity>
        <activity android:name=".movingandpacking2" />
        <!--
             The API key for Google Maps-based APIs is defined as a string resource.
             (See the file "res/values/google_maps_api.xml").
             Note that the API key is linked to the encryption key used to sign the APK.
             You need a different API key for each encryption key, including the release key that is used to
             sign the APK for publishing.
             You can define the keys for the debug and release targets in src/debug/ and src/release/.
        -->
        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/google_maps_key" />

        <activity
            android:name=".map8"
            android:label="@string/title_activity_mpas8" />
        <activity
            android:name=".maps7"
            android:label="@string/title_activity_mpas7" />
        <activity
            android:name=".maps6Activity"
            android:label="@string/title_activity_maps6" />
        <activity
            android:name=".Maps5Activity"
            android:label="@string/title_activity_maps5" />
        <activity
            android:name=".map4Activity"
            android:label="@string/title_activity_map4" />
        <activity
            android:name=".map3Activity"
            android:label="@string/title_activity_map3" />
        <activity android:name=".DisplayDistanceActivity" />
        <activity android:name=".bikemessenger" />
        <activity android:name=".bikecourier" />
        <activity android:name=".driverprofile" />
        <activity android:name=".vehicleregisteration" />
        <activity android:name=".cramessengeractivity" />
        <activity android:name=".essentialdeliveryitemsdetails" />
        <activity
            android:name=".custmoermapactivity"
            android:label="@string/title_activity_custmoermapactivity" />
        <activity
            android:name=".drivermapactivity"
            android:label="@string/title_activity_drivermapactivity" />
        <activity android:name=".map" />
        <activity android:name=".map2" />
        <activity android:name=".driverregisterationactivity" />
        <activity android:name=".customermainactivity" />
        <activity android:name=".Customer_registeration" />
        <activity android:name=".customerloginactivity" />
        <activity android:name=".driverloginactivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".splashscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>